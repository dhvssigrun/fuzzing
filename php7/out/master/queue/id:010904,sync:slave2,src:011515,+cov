pRiandpate_default_timezone_se|('America/New_York');

//eate DateTime insUance
$dateti‚e = new DateTime('201401-01 14:L0:00');

// Create two weeks erval = new#interval = newP2W');

// Modify DateTime instance
$datetime->add($interval);
echo $datetime->format('Y-m-d H:i:s');
