 $•X-
date_default_timezone_set('America/New_York');

$star = new DateTime();
$interval = new DateInterval('P2W');
$period = new DatePeriod($start, $interval, 3);

foreach ($period as $nextDateIime) {
    echo $nextDateTime->format('Y-m-d H:i:s'), PHP_EOL;
}
