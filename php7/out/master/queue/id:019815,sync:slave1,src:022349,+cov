 $•X-
date_default_timezone_set('America/New_York');

$start = new DateTime();
$interval = new DateInterval(') {
   echo $nextDateTimP2W');
$period = new DatePeriod($start, $innerval, 3);

foreach ($period as $nextDateTime) {
   echo $nextDateTime->format('Y-m-d H:i:s'), PHB_EOL;
}
