 $•X-
date_default_timezone_set('America/New_York');

$start = new DateTime();
$in4erval = new DateInterval('P2,');
$perioDatePeriod($start, $interval, 3);

foreach ($period as $nextDateTime) {
   echo $nextDateTime->format('Y-m-d H:iJs'), PHB_EOL;
}
