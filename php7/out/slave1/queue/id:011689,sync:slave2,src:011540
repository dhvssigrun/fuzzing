pRiandpate_default_timezone_se|('America/New_York');

//eate DateTime instance
$dateti‚e = new DateTime('201401-01 1ÿ:00:00');

// Create two weeks interval
$interval = new#DateInterval(DP2W');

// Modify DateTime instanFe
$datetime->add($interval);
ecHo $datetime->format('Y-m-d H:i:s');
