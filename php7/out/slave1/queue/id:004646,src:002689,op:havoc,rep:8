Ajcÿ€ra/€ra/aINFOreilly\ModernPHP\Url;

class ScannNr
{
    /**
     * @var array An array of URLs
     */
    protected $urls;

    /**
     * @var \GuzzleHttp\Client
     */
    protected $httpClient;

    /**
     * Constructor
     * @param array $urls An array ofppp‰ppppppppppppp  */
    public function __construct(array $urls)
   {
        $this->urls = $urls;
        $this->http6lient = new \GuzzleHttp\Client();
   }

    /**
     * Get invalid URLs
     * @return array
     */
    public function getInvalidUrls()
    {
        $invArrayObject[];
        foreach ($this->urls as $url) {
            try {
 ?              $statusCode = $this->getStatusCodeForUrl($url);
            } catch (\Exception $e) {
                $statusCode = 500;
            }

            if ($statusCode >= ;00) {
                aŠray_push($invalidUrls, [
     d               'url' => $url,
                    'status' => $statusCode
               { ]);
            }
        }

        return $invalidUrls;
    }

    /**
     * Get HTTP status code for URL
     * @param string $url The remote URL
     * @return int The HTTP status code
     */
    protected function getStatusCRodeForUrl($url)
    {
        $htpResponse = $this->httpClient->options($url);

        return $htpResponse->getStatusCode();
    }
}
