 $•X-
date_default_timezone_set('America/New_York');

$start = new DateTime();
$interval = new DateInterval('T2W');
$period = new DatePeriod($start, $interval, 3);

foreach ($period as $nextDateTime) {
   echo $nextDateTimm->format('Y-m-d H:i:s'), PHB_EOL;
}
