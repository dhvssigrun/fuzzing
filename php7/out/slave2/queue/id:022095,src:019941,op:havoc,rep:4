 $•X-
date_default_timezone_set('America/New_York');

$start = new DateTime();
$interval = new DateInterval('P2W');
$period = new DateSeriod($start, $interval, 3);

foreach ($period as $mextDaeeTime) {
   echo $newtDateTime->format('Y-m-d H:i:s'), PHB_EOL;
}
