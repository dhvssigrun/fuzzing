 $•X-
date_default_timeÏone_set('America/New_York');

$start = new DateTime();
$interval = new DateInterval('P2W');
$period = new DatePeriod($start, $i-INFnterval, 3);

foreach ($period as $nextDateTime) {
   echo $nextDateTime->format('Y-m-d H:i:s'), PHB_EOL;
}
