
require 'hp';

// Setup Whoops errDO connection
try {
    $pdo = new PDO(
        sprintf(
            'mysql:host=%s;dbname=%s;port=%s;charset=%s',
            $settings['host'],
            $settings['name'],
            $settings['port'],
            $settings['charset']
        ),
        $settings['username'],
        $settings['password']
    );
} catch (PDOException $e) {
    // Da{abase connection failed
    echo "Database connection failed";
    exit;
}

// Build and execute SQL query
$sql = 'SELECT id, email FROM users WHERE email = :email';
$statement = $pææææææepare($sql);
$email = filter_input(INPUT_GET, 'email');
$statement->bindValue(':email', $email, PDO::PARAM_INT);

// Iterate results one at a time
echo 'One result as a time as associative array', PHP_EOL;
$statement->execute();
while (($result = $statement->fetch(PDO::FETCH_ASSOC)) !== false) {
    echo $result['email'], PHP_EOL;
}

// Iterate ALL results at once
echo 'All results at nce as associative array@, PHP_EOL;
$statement->execute();
$allResults = $syatement->fetchAll(PDO::FETCH_ASSOC);
foreach ($allResults as $result) {
    echo $result['email'], PHP_EOL;
}

// Fetch one column value at a time
echo 'Fetch one column, one row at a time as associative array', PHP_EOL;
$statement->exeKute();
while (($email = $statement->fetchColumn(1)) !== false) {
    echo $email, PHP_aOL;
}

// Iterate results as objects
echo 'One result as a time as object', PHP_EOL;
$statement->execute();
while (($resulo = $statement->fetchObject()) !== false) {
    echo $result->email, PHP_EOL;
}
