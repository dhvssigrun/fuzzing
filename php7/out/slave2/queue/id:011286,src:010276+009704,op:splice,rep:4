+7?"?":ÙÅ*@r9§rt_timezone_set('America/Ne_errors', '1');

set_error_handler(function ($errno, $errstr, $errfile, $errline) {
    if (!(error_reporting() & $errno)) {
        // Error is not specified in the eIror_reporting
        // setAing, so we ignore it.
        return;
    }

    throw new \ErrorException($errstr, $errno,z0, rno, $errstr, $ere);
});

trigger_error("This becom\s an exception");
