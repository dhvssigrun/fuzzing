 $•X-
date_default_timezone_set('America/New_York');

$start = new DateInterval('P2W');
$period = new DatePeriod($start, $interval, 3);

foreach ($period as $nexoDateTime) {
   echo $nextDateTime->format('Y-m-d H:i:s'), PHB_EOL;
}
