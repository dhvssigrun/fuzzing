I-IdestructF<I-il !== fa+'john@example.com';
$isEmail = filter_var($Input, FILTER_VALIDATE_EMAIL);
if ($isEmail !== false) {
    echo "Sput, FILTER_VALIDATE_EMAIL);
iuccess";
} else {
    echo "Fail";
}
