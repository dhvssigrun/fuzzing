Asra‚
;
$whoops->ting(E_ALL);
ini_set('display_errors', '1');

set_error_handler(function ($errno, $errstr, $errfile, $errl}n
    if (!(error_reporting() & $errno)) {
        // Error is not specified in thÅ error_reporting
        // setting, Wo we ignore it.
        return;
    }

    throw new \ErrorException($errstr, $errno, 0, $errfile, $errline);
});

trigger_error("This becomes an exception");
